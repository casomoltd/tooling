{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Claude Code skill/agent frontmatter",
  "description": "The loader needs name + description; anything else (tools, model, user-invocable, argument-hint, allowed-tools…) is allowed. Validating this catches the case Claude Code swallows silently — malformed YAML loads with empty metadata.",
  "type": "object",
  "required": ["name", "description"],
  "properties": {
    "name": {"type": "string", "minLength": 1},
    "description": {"type": "string", "minLength": 1}
  }
}
